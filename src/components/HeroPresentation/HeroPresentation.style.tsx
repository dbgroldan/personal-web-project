import styled from "styled-components";

const CarouselContainer = styled.div`
    min-height: 500px;
`;

const HeroContainer = styled.div`
    max-width: 55%;
    min-width: 5%;
`;

const AboutMeButtons = styled.div`
    display: flex;
    gap: 10px;
`;

export { AboutMeButtons, HeroContainer, CarouselContainer };
