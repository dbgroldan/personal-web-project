import styled from "styled-components";

const Tag = styled.div`
    margin: 3px;
`;

const ImageContainer = styled.figure`
    max-width: 30%;
    max-height: 30%;
`;

export { Tag, ImageContainer };
