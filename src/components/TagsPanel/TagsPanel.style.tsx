import styled from "styled-components";

const Container = styled.div`
    display: flex;
    flex-wrap: wrap;
`;

const Card = styled.div`
    margin-top: 15px;
`;

const Tag = styled.div`
    margin: 3px;
`;

export { Container, Card, Tag };
